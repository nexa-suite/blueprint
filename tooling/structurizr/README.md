# Structurizr Local

Official Structurizr Local Docker workflow for the canonical C4 source, pinned to `structurizr/structurizr:2026.06.28`. Port `9090` maps to the container service port. Start only this service when inspecting architecture diagrams; validation is handled by the repository validator.

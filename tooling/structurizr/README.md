# Structurizr Local

Official Structurizr Local Docker workflow for the canonical C4 source, using the current `structurizr/structurizr` image and `local` command. Port `9090` maps to the container service port. Start only this service when validating architecture diagrams.

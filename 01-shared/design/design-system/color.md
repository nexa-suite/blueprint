# Color

Use semantic roles for surface, text, border, action, status and data
visualization. Semantic color must preserve meaning in themes, contrast, focus
indication and non-color redundancy.

Design Lab validates WCAG-oriented contrast. Product status semantics remain
separate from visual color semantics.
